package com.projectx.game.hooks.impl

// Placeholder for a potential jag::InterfaceManager::GetInterface hook. The previous
// exploratory implementation (a commented-out debug hook) carried a hardcoded address
// literal and has been removed. If this hook is ever needed, add the target to the
// OFunctions block of the offset tables and reference it via @Hook("<NAME>") — never
// hardcode an address here.
object GetInterface

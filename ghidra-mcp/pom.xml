<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>
  <groupId>com.lauriewired</groupId>
  <artifactId>GhidraMCP</artifactId>
  <packaging>jar</packaging>
  <version>1.0-SNAPSHOT</version>
  <name>GhidraMCP</name>
  <url>http://maven.apache.org</url>

  <properties>
    <!-- Ghidra install to compile against. build-install.sh exports GHIDRA_INSTALL_DIR. -->
    <ghidra.dir>${env.GHIDRA_INSTALL_DIR}</ghidra.dir>
    <ghidra.version>12.1</ghidra.version>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencies>
    <!-- Ghidra module JARs as system-scoped deps, resolved from the local $GHIDRA_INSTALL_DIR (12.1). -->
    <dependency>
      <groupId>ghidra</groupId><artifactId>Generic</artifactId><version>${ghidra.version}</version>
      <scope>system</scope>
      <systemPath>${ghidra.dir}/Ghidra/Framework/Generic/lib/Generic.jar</systemPath>
    </dependency>
    <dependency>
      <groupId>ghidra</groupId><artifactId>SoftwareModeling</artifactId><version>${ghidra.version}</version>
      <scope>system</scope>
      <systemPath>${ghidra.dir}/Ghidra/Framework/SoftwareModeling/lib/SoftwareModeling.jar</systemPath>
    </dependency>
    <dependency>
      <groupId>ghidra</groupId><artifactId>Project</artifactId><version>${ghidra.version}</version>
      <scope>system</scope>
      <systemPath>${ghidra.dir}/Ghidra/Framework/Project/lib/Project.jar</systemPath>
    </dependency>
    <dependency>
      <groupId>ghidra</groupId><artifactId>Docking</artifactId><version>${ghidra.version}</version>
      <scope>system</scope>
      <systemPath>${ghidra.dir}/Ghidra/Framework/Docking/lib/Docking.jar</systemPath>
    </dependency>
    <dependency>
      <groupId>ghidra</groupId><artifactId>Decompiler</artifactId><version>${ghidra.version}</version>
      <scope>system</scope>
      <systemPath>${ghidra.dir}/Ghidra/Features/Decompiler/lib/Decompiler.jar</systemPath>
    </dependency>
    <dependency>
      <groupId>ghidra</groupId><artifactId>Utility</artifactId><version>${ghidra.version}</version>
      <scope>system</scope>
      <systemPath>${ghidra.dir}/Ghidra/Framework/Utility/lib/Utility.jar</systemPath>
    </dependency>
    <dependency>
      <groupId>ghidra</groupId><artifactId>Base</artifactId><version>${ghidra.version}</version>
      <scope>system</scope>
      <systemPath>${ghidra.dir}/Ghidra/Features/Base/lib/Base.jar</systemPath>
    </dependency>
    <dependency>
      <groupId>ghidra</groupId><artifactId>Gui</artifactId><version>${ghidra.version}</version>
      <scope>system</scope>
      <systemPath>${ghidra.dir}/Ghidra/Framework/Gui/lib/Gui.jar</systemPath>
    </dependency>

    <dependency>
      <groupId>junit</groupId><artifactId>junit</artifactId><version>3.8.1</version><scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <artifactId>maven-jar-plugin</artifactId>
        <version>3.2.2</version>
        <configuration>
          <archive>
            <manifestFile>src/main/resources/META-INF/MANIFEST.MF</manifestFile>
          </archive>
          <finalName>GhidraMCP</finalName>
          <excludes>
            <exclude>**/App.class</exclude>
          </excludes>
          <outputDirectory>${project.build.directory}</outputDirectory>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-assembly-plugin</artifactId>
        <version>3.3.0</version>
        <configuration>
          <descriptors>
            <descriptor>src/assembly/ghidra-extension.xml</descriptor>
          </descriptors>
          <finalName>GhidraMCP-${project.version}</finalName>
          <appendAssemblyId>false</appendAssemblyId>
        </configuration>
        <executions>
          <execution>
            <id>make-assembly</id>
            <phase>package</phase>
            <goals><goal>single</goal></goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>3.1.2</version>
        <executions>
          <execution>
            <id>copy-dependencies</id>
            <phase>prepare-package</phase>
            <goals><goal>copy-dependencies</goal></goals>
            <configuration>
              <outputDirectory>${project.build.directory}/lib</outputDirectory>
              <includeScope>runtime</includeScope>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
